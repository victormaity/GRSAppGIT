          SELECT listagg(deal_id,',') within group (order by deal_id)         FROM         (SELECT *         FROM LS_SRV.JPA_DEAL_EVENT jde         JOIN LS_SRV.JPA_DEAL_EVENT_DRUG jded         ON jde.event_id        = jded.event_id         WHERE jde.stage       IS NOT NULL         AND jde.stage_notes   IS NOT NULL         AND jded.drug_id      IS NOT NULL         AND jded.display_name IS NOT NULL         AND jde.deal_id NOT   IN         (SELECT deal_id         FROM LS_SRV.JPA_DEAL_EVENT jde         JOIN LS_SRV.JPA_DEAL_EVENT_DRUG jded         ON jde.event_id        = jded.event_id         WHERE jde.stage       IS NULL         AND jde.stage_notes   IS NULL         AND jded.display_name IS NOT NULL         )         ORDER BY dbms_random.value         )         WHERE rownum<6     