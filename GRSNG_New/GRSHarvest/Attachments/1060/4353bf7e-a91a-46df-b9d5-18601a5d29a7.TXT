          SELECT DRUG_ID         FROM ls_srv.jp_drug_development_history         WHERE reference_id IS NOT NULL         AND status_date    IS NOT NULL         AND rownum<10         ORDER BY dbms_random.value     