          SELECT listagg(deal_id,',') within group (order by deal_id)         FROM         (SELECT prin.deal_id         FROM LS_SRV.JPA_DEAL_PROJECTED_TO_PRIN prin         JOIN LS_SRV.JPA_DEAL_PROJECTED_TO_PART part         ON prin.deal_id = part.deal_id         JOIN LS_SRV.JPA_DEAL_VALUES_MAX_MIN dvmm         ON prin.deal_id          = dvmm.deal_id         WHERE prin.reference_id IS NOT NULL         AND prin.payment_type   IS NOT NULL         AND prin.accuracy       IS NOT NULL         AND prin.amount         IS NOT NULL         AND part.reference_id   IS NOT NULL         AND part.payment_type   IS NOT NULL         AND part.accuracy       IS NOT NULL         AND part.amount         IS NOT NULL         AND proj_prin_max       IS NOT NULL         AND proj_prin_min       IS NOT NULL         AND proj_prin_acc       IS NOT NULL         AND proj_part_max       IS NOT NULL         AND proj_part_min       IS NOT NULL         AND proj_part_acc       IS NOT NULL         AND paid_prin_max       IS NOT NULL         AND paid_prin_min       IS NOT NULL         AND paid_prin_acc       IS NOT NULL         AND paid_part_max       IS NOT NULL         AND paid_part_min       IS NOT NULL         AND paid_part_acc       IS NOT NULL         ORDER BY dbms_random.value         )         WHERE rownum<         6     