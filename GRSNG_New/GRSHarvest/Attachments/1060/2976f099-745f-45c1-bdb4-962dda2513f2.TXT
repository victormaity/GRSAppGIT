          SELECT listagg(deal_id,',')within group (order by deal_id)         FROM         (SELECT jd.deal_id         FROM LS_SRV.JPA_DEAL jd         JOIN LS_SRV.JPA_DEAL_PATENT jdp         ON jd.deal_id                 = jdp.deal_id         WHERE jd.title               IS NOT NULL         AND jd.principal_company_id  IS NOT NULL         AND jd.partnering_company_id IS NOT NULL         AND jd.deal_type             IS NOT NULL         AND jd.deal_status           IS NOT NULL         AND jd.start_date            IS NOT NULL         AND jd.end_date              IS NOT NULL         AND jd.event_date            IS NOT NULL         AND jd.is_optional           IS NOT NULL         AND jd.summary               IS NOT NULL         AND jd.added_date            IS NOT NULL         AND jdp.patent_id            IS NOT NULL         ORDER BY dbms_random.value         )         WHERE rownum<         5     