          SELECT * FROM(SELECT dd.drug_id AS "drugid"         FROM JP_NG_Fast.drug_development dd         JOIN JP_NG_Fast.development_status ds         ON (dd.status_id = ds.status_id)         JOIN JP_NG_Fast.development_status_category dsc         ON (dd.status_id       = dsc.status_id)         WHERE dsc.category_id IN ('INA','ACT')         AND ds.status_name     ='Launched'         GROUP BY dd.drug_id         ORDER BY dbms_random.value)where rownum =1     