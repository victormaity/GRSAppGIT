          select * from (select drugid from ( SELECT dcp.drug_id AS drugid         FROM ls_srv.drug_company_primary dcp         JOIN ls_srv.company co ON (co.company_id = dcp.company_id)         ORDER BY dcp.drug_id,co.company_id )order by dbms_random.value)where rownum<2     