          SELECT *         FROM         (SELECT *         FROM         (SELECT pf.family_id         FROM ls_srv.ng_pf_patent_basic pf         JOIN         (SELECT patent_id         FROM ls_srv.jp_patent         WHERE rownum           < 100         AND inventors_abstract IS NOT NULL         ) p         ON (pf.patent_id           = p.patent_id)         WHERE upper(pf.is_primary) = 'Y'         ORDER BY dbms_random.value         )         WHERE rownum <1000         )         WHERE rownum  <2     