using System;
using System.Linq;
using System.Security.Principal;
using GlobalReportingSystem.Core.Models.Entities;
//using GlobalReportingSystem.Data.DB;

namespace GlobalReportingSystem.Core.Models.GRS.DB
{
    public static class DbExtentions
    {
      /*  public static UserSession GetSession(this GRSDataBaseEntities db, IPrincipal user)
        {
            var guid = Guid.Parse(user.Identity.Name);
            return db.UserSessions.FirstOrDefault(p => p.SessionID == guid);
        }*/

    }
}